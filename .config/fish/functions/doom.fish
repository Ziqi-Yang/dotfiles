function doom
    # try both if error occured
    # proxychains ~/.emacs.d/bin/doom $argv
    # env all_proxy=http://127.0.0.1:7890 ~/.emacs.d/bin/doom $argv
    
    # set git proxy is the best way
    ~/.emacs.d/bin/doom $argv
end
